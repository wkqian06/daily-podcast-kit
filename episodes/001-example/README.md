# Example episode

A real episode, kept as a format reference. `script.txt` and `episode.json` are the only two
files you write by hand; everything under `audio/` is produced by the pipeline.

To hear it, run the production steps from `CLAUDE.md` against this directory.
