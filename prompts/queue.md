# Topics you want, one per line, used top-down: the nightly job takes the first line and proposes
# two or three angles on it. The line is deleted only once you confirm one and the episode is
# written, so a proposal you throw away keeps its topic queued. Lines starting with # are ignored.
# A line can be a bare subject, a subject with the angle you want, or a path to a file to read.
# Empty queue: the candidates come from the library's open questions (PALACE_DIR). With neither,
# the night ends without a proposal — nothing is invented to fill the slot.
