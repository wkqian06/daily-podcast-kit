{
  "name": "private-podcast-worker",
  "private": true,
  "devDependencies": { "wrangler": "^4" },
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy"
  }
}
